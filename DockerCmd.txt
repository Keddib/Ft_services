docker top [id] # see proccesses running om a container

#images
Docker images
docker images -q
docker rmi [images-id]
docker rmi $(docker images -q)
docker image prune

#containers
docker ps
docker ps -a
docker rm [container id]
docker stop [c id]
docker ps -a -q #id of all containers
docker rm $(docker ps -a -q)
#images history
docker image history [img-id] # show layers of an image for full commands use --no-trunc flug
docker image save [img-id] # creat a tar file that has all the layers bundled in it 

#run containers
docker run --rm -it [img-id or name] sh

#docker kill
docker kill [c id]
#inspect 
docker inspect #this gives u more info about a container

#exec run a command on a running container

docker exec -it [i id] [cmd]

#history We'll get into layers in a bit but this allow you to see how this Docker image's layer composition has changed over time and how recently.
docker history [c id/name]
docker info # Dumps a bunch of info about the host system. Useful if you're on a VM somewhere and not sure what the environment is.

